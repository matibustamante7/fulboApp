import { Container, Typography } from "@mui/material";

export default function Footer() {
    return(
        <Container maxWidth sx={{textAlign:'center', }}>
            <Typography variant="h6">Todos los derechos reservados. Matias Bustamante</Typography>
        </Container>
    )
}